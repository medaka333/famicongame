# CLAUDE.md — famicongame

## 最重要: このプロジェクトには「祭り出店向けWeb版」という別の実体がある。ソースを直しただけでは配布物は直らない

`web/breakout/index.html` と `web/stg/index.html`(および同じフォルダの `index.pck`/`index.wasm`等)は、Godotプロジェクトの**ビルド成果物のスナップショット**であり、`scripts/`や`scenes/`のソースを編集しても自動的には更新されない。ユーザーは実際のプレイ確認をこのWeb版で行っていることが多い(祭り出店で配る想定のビルドだから)。

**実際にあった事故:** チュートリアルのバグを2回に渡って直し、Godotエディタ上のheadless検証・スクリーンショットで入念に確認して「直りました」と報告したが、ユーザーには「直ってない」と何度も言われた。原因は、Web版のビルド日時が6月(`web/breakout/index.html`)・6月4日(`web/stg/index.html`)のまま何ヶ月も更新されておらず、**ユーザーがテストしていたWeb版には、その日行った修正が1行も反映されていなかった**ことだった。ソース側の修正自体は最初から正しかった。

**このため、ソースコード(`scripts/`, `scenes/`)を編集してユーザー向けに「直った」と報告する前に、必ず次を行う:**
1. `ls -la web/breakout/ web/stg/` 等でビルド成果物の**タイムスタンプ**を確認し、直近の編集より古ければ**ビルドが古い(=ユーザーが見ているものに反映されていない)ことを疑う**。
2. 該当するWeb版を再エクスポートする(下記コマンド)。テストや報告は、再エクスポート後の成果物に対して行う。
3. 可能なら実際にローカルHTTPサーバーで配信し、ブラウザで(claude-in-chromeで)実際に操作して確認する。Godotエディタ上のheadless検証だけでなく、**実際にユーザーが触る配布物そのもの**を確認するのが一番確実。

```
# Web版の再エクスポート(headless、GUIなし)
"C:\Users\user\Projects\GodotEngine\Godot_v4.6.3-stable_win64_console.exe" --headless --path . --export-release "Breakout (Web)" web/breakout/index.html
"C:\Users\user\Projects\GodotEngine\Godot_v4.6.3-stable_win64_console.exe" --headless --path . --export-release "Web" web/stg/index.html
```

- エクスポート前にプロジェクトルートに検証用の一時スクリプト(`_shot*.gd`等)を残さないこと。エクスポートに巻き込まれて成果物に混入する。
- ローカルで配信するときは `py -m http.server <port>`(このマシンには`python`本体はないが`py`ランチャーは使える)を`web/`ディレクトリで実行し、`http://127.0.0.1:<port>/breakout/index.html` 等をclaude-in-chromeで開く。使い終わったら`TaskStop`でサーバーを止める。
- Web版はAudioWorkletの読み込みでエラーが出ることがある(ローカルの簡易サーバーが必要なHTTPヘッダーを返さないため)が、これは音声まわりの既知の制約であり、キー入力や描画のバグとは無関係。コンソールエラーを見て毎回慌てて追わないこと(ただし`InputMap action "ui_select_start" does not exist`のような、明らかにコード側の参照ミスに見えるエラーは別途調査対象としてメモしておく)。
- ブラウザ自動操作ツールでの矢印キー「タップ」は、実際に人間が押すキー入力より遥かに短い(ダウン+アップが1フレームにも満たない)ことがあり、`Input.is_action_pressed()`のようなポーリング型の判定を拾えないことがある。これはツール側の制約であり、ゲームのバグではない可能性が高い — 判断がつかない場合はGodotネイティブのheadless検証(`Input.action_press()`→数フレーム待つ→`Input.action_release()`)の結果を優先する。

## バグ修正を「直った」と報告する前に、必ずこの手順を全部やる(スキップ禁止)

これは努力目標ではなく必須手順。1つでも飛ばした状態で「直りました」と報告することを禁止する。

1. **直す前に、そのコードが持っていた"仕様"を一行で書く。** 例:「シュートボタンを押したらstuckなボールを発射する」。この一行を書かずに修正コードを書き始めない。
2. **修正の種類を自己申告する。** 次のどちらかを自分で判定してから直す:
   - (a) 根本原因を直す修正(状態を作っている場所そのものを直す)
   - (b) ガード/早期return/条件分岐を追加する対症療法(症状が出る条件を潰すだけ)
   - **(b)を選んだ場合は必ず(a)ができないか一度検討し、できるならそちらを選ぶ。** (b)のまま進めるのは、根本原因の修正が明らかに過剰な範囲変更になる場合のみ。
3. **(b)のガード/early returnを追加したら、そのガードが止めている分岐で、1で書いた"本来の仕様"が実行されなくなっていないかをその場で確認する。** 「このifを通らなくする」と書いたら、次の行で「このifの中で本来実行されるはずだった処理は何か、それは誰も呼ばなくなっていないか」を自問して答えを書き出す。答えられない/自信がないなら(a)に切り替える。
4. **修正後、バグが起きていた操作を再現して直ったことを確認するのに加えて、そのすぐ隣にある正常系の操作も同じ手順で再現して壊れていないか確認する。** 「バグが消えた」だけでなく「壊れていなかった機能が今も動くか」を必ず見る。今回の実例では、バグ(変な位置から出る)は消えたが、隣接する正常系(発射ボタンを押すとボールが発射される、というチュートリアルの核心)を壊していた。この隣接チェックを省略したのが失敗の直接原因。
5. **「視覚的に確認しろ」と言われたら、確認する対象は「異常なものが写っていないか」ではなく「その画面が本来ユーザーに見せるべき体験(操作の結果として起きるはずの動き・演出)が実際に起きているか」。** スクリーンショットを並べて、その一連の流れを他人に実況できるレベルで説明できなければ「確認した」と言わない。
6. 上記1〜5を実施した内容を、報告文に短く含める(「〜の仕様を確認し、隣接する〜も検証した」等)。省略して「確認しました」とだけ書かない。

## Godotスクリプトを検証するときの必須ルール

**「動くはず」で報告しない。実際に動かして確認してから報告する。** これは過去に実際の見落としがあった教訓に基づく。

### 実例(2026-08-24): ブロック崩しチュートリアル終了時のボール誤発射バグ

チュートリアル終了直後、ボールが画面左上の「変なところ」から飛び出すバグが本番に混入した。原因は `_unhandled_input` 内で、同じshootイベントの中で「①チュートリアル終了処理(新しいボールを再生成)」→「②stuckなボールを`launch()`する処理」の順で両方実行されてしまい、②が①で生成された直後(position未確定)の新ボールを誤って発射していたこと。

このバグを最初の実装時に見逃した理由:
- 検証に「ポーズキーでチュートリアルをスキップ」する経路しか使っていなかった。スキップ経路はこの`_unhandled_input`のshoot分岐を一切通らないため、バグのあるコードパスを一度も実行していなかった。
- 位置のログではなく「チュートリアルが終わったかどうか」の真偽値だけを確認して満足していた。

**教訓: 「デバッグ用のショートカットで最短で終端状態に到達して確認する」だけでは不十分。実際にユーザーがたどる入力シーケンス(この場合は「移動してから発射する」という自然な操作)を必ず一度は再現して検証すること。** ステージ遷移・チュートリアル完了・アイテム取得などの「複数の状態変化が同じ入力イベントで連鎖しうる」箇所は特に要注意 — イベントハンドラ内で状態を変える処理(シーン再構築、オブジェクト再生成など)を呼んだ直後に、同じハンドラの後続コードが「変更前の前提」で動いていないか必ず読み返す。

### 実例その2(同日): 「視覚的に確認した」と言いながら、確認していたのは無関係なバグだけだった

上のバグを直した直後、ユーザーから「チュートリアルで移動→発射ボタンを押したら即画面が切り替わる、弾が出るのを見せる必要があるだろ」という指摘が来た。実際に起きていたこと: 直した「誤発射バグ」の対処療法として `_unhandled_input` に「チュートリアルを終わらせたshootイベントではボールを発射しない」というガードを入れていた。これにより**位置バグは消えたが、代わりに「チュートリアル完了の合図となるはずの発射が実際には一度も起きない」という別の欠陥を生んだ**。スクリーンショットも撮って「左上に変なボールが出ていないこと」は確認したが、**「発射ボタンを押したら実際にボールが画面を飛んでいくか」という、その画面が本来見せるべき肝心の演出そのものは見ていなかった。**

**教訓:**
- 「視覚的に確認しろ」と言われたときにチェックすべきは、**技術的な異常(変な位置に何か出ていないか)だけでなく、その画面/操作が本来ユーザーに見せるべき体験が実際に起きているか**。「バグらしきものが写っていない」で満足せず、「これは意図通りの演出になっているか」を毎回自問する。
- 症状(変な位置からボールが出る)を消すために対症療法的なガード(「このタイミングでは発射しない」)を足すときは、**そのガードが機能を殺していないか**を必ず考える。今回の場合、シュートボタンの本来の役割(ボールを撃つ)を、たった一つの特殊ケース(チュートリアル終了と重なった瞬間)でだけ無効化しており、それこそが一番プレイヤーの目に入る重要な瞬間だった。
- 正しい修正は「症状を止める」ではなく「なぜ壊れているかの根本原因を直す」こと。今回の根本原因は「チュートリアル終了時に練習用のボールを破棄して新しいボールを作り直していたため、新ボールの位置が未確定なまま発射されていた」ことであり、正しい直し方は「新しいボールを作らず、練習用のボール/パドルをそのまま本編に引き継ぐ」だった。これなら発射も自然に起こり、位置バグも原理的に発生しなくなる。

## headlessでのGodot動作検証(音を出さない)

ユーザーはこの検証中に音が鳴るのを嫌う。**必ず `--audio-driver Dummy` を付ける。** ビルド済みexeは `C:\Users\user\Projects\GodotEngine\Godot_v4.6.3-stable_win64_console.exe`。

`SceneTree` を継承したカスタムスクリプトを `-s` で実行し、`_initialize()`でシーンを`root.add_child()`、`_process(delta)`でフレームを進めながら`Input.parse_input_event()`でキー/アクションイベントを注入し、最後に`root.get_texture().get_image().save_png()`でスクリーンショットを撮る、という手法が有効。

```
godot.exe --audio-driver Dummy --path . -s res://_shot.gd -- res://scenes/xxx/Yyy.tscn user://out.png [frames]
```

- Autoload(`GameState`等)はグローバル識別子としては使えない。`root.get_node("GameState")` で明示的に取得すること。
- 検証用に作った一時スクリプト(`_shot*.gd`等)とプロジェクトルートに残る`.uid`ファイルは、検証が終わったら必ず削除する。`user://` 配下に吐いたスクリーンショットPNGも同様に消す。
- Godotプロセスは検証後に**強制終了しない**。ユーザーがエディタを開きっぱなしにしていることがあり、落とすと開き直す手間をかけさせる。放置してよい。
- `--import`(headlessでのアセット再インポート)を走らせると `project.godot` の `config/features` やアセットの `.import` ファイルの`path`ハッシュが書き換わることがある。検証目的だけであれば `git checkout --` で元に戻すこと(コミットしない)。

### このheadless手法特有の既知の落とし穴: キー入力タイミングのフレーキーさ

`Input.parse_input_event()` で注入したキー/物理キーの押下状態が、**同じコードでも実行するたびに反映されるまでのフレーム数が大きくばらつく**(数フレームで反映されることもあれば、1800フレーム(30秒以上)保持しても一切反映されないことがある)。これはウィンドウのOSフォーカスの取り合いなど、テスト環境側の問題であり、ゲームコード側のバグではないことが多い。

**このため:**
- 固定フレーム数で「反応しなければ失敗」と判定しない。**成立を待つポーリングループ**にし、タイムアウトは十分長く(1800フレーム程度)取る。
- 1回失敗しても「バグだ」と即断しない。**同じテストを単体で(他のステップと混ぜずに)再実行**して切り分ける。複数ステップを1つのスクリプトに詰め込むと、前段のキー解放漏れ・状態の持ち越しでテスト側のバグが混入しやすい(実際に本セッションで発生した: 複数ステップを繋げたテストで1ステップ目が失敗したが、同じ操作を単体テストにしたら即座に成功した)。
- 逆に「1回成功した」だけで安心せず、疑わしければ複数回・複数の切り口(単体テスト、実際の操作シーケンスを模したテスト、スクリーンショットでの目視)で確認してから報告する。
